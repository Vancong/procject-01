
header.header
  .container-fluid
    .row.align-items-center
      .col-3
        .inner-logo
          a(href="/admin/dashboard") ADMIN
      .inner-logo 
      .col-9
        div(class="col-9 text-right mr")
          a(
            href=`${prefixAdmin}/profile`
            class="btn btn-info btn-sm"
          ) #{account.fullName}
          a(
            href=`${prefixAdmin}/authen/logout`
            class="btn btn-danger btn-sm ml-2"
          ) Đăng xuất