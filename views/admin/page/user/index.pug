extends ../../layout/default.pug
block main
    h1(class="mb-4") Danh sách tài khoản User
    .card.mb-3
      .card-header Danh sách
      .card-body
        .row
          .col-8
          .col-4.text-right
        table(
          class="table table-hover table-sm"
        )
          thead
            tr
              th STT
              th Họ tên
              th Email
              th Trạng thái
              th Hành động

          tbody
            each item, index in userAccount
              tr 
                td #{index+1}
                td #{item.fullName}
                td #{item.email}
                td
                    if (item.status === "active")
                        button(
                          class="badge badge-success"
                          data-status="inactive"
                          linkUser=`${prefixAdmin}/user/inactive/${item.id}`
                        ) Hoạt động
                    else
                        button(
                          class="badge badge-danger"
                          data-status="active"
                          linkUser=`${prefixAdmin}/user/active/${item.id}`
                        ) Dừng hoạt động
                td
          
                  a(
                    class="btn btn-warning btn-sm ml-1"
                    href=`${prefixAdmin}/user/edit/${item.id}`
                  ) Sửa

                  button(
                    class="btn btn-danger btn-sm ml-1"
                    button-deleteAcc=item.id
                    link=`${prefixAdmin}/deleted/${item.id}`
                  ) Xóa